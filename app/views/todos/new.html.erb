<h1>Create new todo</h1>

<%= form_for(@todo) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.check_box :status %>

    <%= f.submit "Create" %>
<% end %>
<%= link_to 'Back', todos_path %>